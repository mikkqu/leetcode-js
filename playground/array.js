
let arr = [0, 1, 2]

/*
    [0, 1, 2]

    el = arr.pop()          // 2
    el = arr.shift()        // 0

    arr.push(5, 6)          // 0, 1, 2, 5, 6
    arr.unshift(5, 6)       // 5, 6, 0 1, 2
*/

console.log(arr.toLocaleString())